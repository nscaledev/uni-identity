apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-organization-user-controller
  labels:
    {{- include "unikorn.labels" . | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Release.Name }}-organization-user-controller
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-organization-user-controller
    spec:
      containers:
      - name: {{ .Release.Name }}-organization-user-controller
        image: {{ include "unikorn.organizationUserControllerImage" . }}
        args:
        {{- include "unikorn.core.flags" . | nindent 8 }}
        {{- include "unikorn.otlp.flags" . | nindent 8 }}
        resources:
          {{- .Values.organizationUserController.resources | toYaml | nindent 10 }}
        securityContext:
          readOnlyRootFilesystem: true
      serviceAccountName: {{ .Release.Name }}-organization-user-controller
      securityContext:
        runAsNonRoot: true
